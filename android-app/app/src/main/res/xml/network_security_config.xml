<?xml version="1.0" encoding="utf-8"?>
<!--
  Default: cleartext (http://) traffic is denied. HTTPS is always allowed.

  Cleartext is permitted only for the loopback / emulator-host shortcuts
  used during local backend development. To talk to a backend on your LAN
  from a physical device, add another <domain> entry below with your
  laptop's LAN IP (e.g. 192.168.1.42). Production must serve HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.68.111</domain>
    </domain-config>
</network-security-config>
